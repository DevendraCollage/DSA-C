//* Write a program to remove duplicates from sorted array.

#include <stdio.h>
void main()
{
    int size;

    printf("Enter The Size of Array : ");
    scanf("%d", &size);

    int arr[size];

    for (int i = 0; i < size; i++)
    {
        printf("%d : ", i);
        scanf("%d", &arr[i]);
    }

    //! From this will incomplete program
}